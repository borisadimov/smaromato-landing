## Посадочная страница для привлечения партнеров Смартомато
### Деплой
Производится через `Grunt`, который ставится командой `npm install -g grunt-cli`
Доступные команды:

```
grunt deploy:overlord
grunt deploy:production
```